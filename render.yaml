databases:
  - name: chess-db
    databaseName: chess_db_qan4
    user: chess_db_qan4_user

services:
  - type: web
    name: web-chess
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mysite
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false